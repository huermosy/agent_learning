# 工具调用与工作流术语表

## 核心术语
- **Function Calling**：模型通过结构化参数调用函数的能力。
- **Tool Schema**：工具参数与描述规范。
- **Workflow State**：任务执行过程中的状态信息。
- **Retry**：失败后重试机制。
- **Idempotency**：重复执行不会产生额外副作用的特性。

## 常见易混淆概念
- **工具调用 vs API 集成**：前者强调模型决策，后者强调程序接入。
- **重试 vs 回滚**：重试是再执行，回滚是撤销状态。
